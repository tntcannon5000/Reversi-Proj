public class Main {
    public static void main(String[] args) {
        // Set the size of the board, for example 8x8
        int boardSize = 8;
        
        // Create a new instance of the src.src.Reversi game with the specified board size
        Reversi game = new Reversi(boardSize);
        
        // Set up the game frame and make it visible
        // game.makeFrame();
    }
}